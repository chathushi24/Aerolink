apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: aerolink
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: 146697354183.dkr.ecr.us-east-1.amazonaws.com/dev-aerolink-notification-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8006
        env:
        - name: AWS_REGION
          value: "us-east-1"
        - name: NOTIFICATIONS_TABLE_NAME
          value: "dev-NotificationsTable"
        - name: SQS_NOTIFICATION_LOGS_QUEUE_URL
          value: "https://sqs.us-east-1.amazonaws.com/146697354183/dev-aerolink-NotificationLogsQueue"
        - name: JWT_SECRET
          value: "aerolink-super-secret-jwt-key-2026"
        livenessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8006
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: aerolink
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8006
      targetPort: 8006
  type: ClusterIP
